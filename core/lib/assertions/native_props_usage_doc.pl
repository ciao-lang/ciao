% Include file: description of how to load the native props library, to be included in each of its parts.

:- doc(module,"These properties are part of the @lib{native_props} library.").

:- doc(usage, "@tt{:- use_module(library(assertions/native_props))}

or also as a package @tt{:- use_package(nativeprops)}.

Note the slightly different names of the library and the package.").

