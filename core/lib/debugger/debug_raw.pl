:- package(debug_raw).
% Internal package for raw debugging (see toplevel_debugger.pl)

% TODO: requires defined(optim_comp), merge?

% TODO: 'dynamic' is no longer needed
% :- '$default_preddef'(dynamic).
